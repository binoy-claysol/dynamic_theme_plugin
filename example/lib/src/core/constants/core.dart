/// {@category core/ constants}
library core_constant;
/// It's a class that contains constants that are used throughout the app
class CoreConstants {
  /// App title constant
  static const appTitle = 'Dynamic Theme';
}

/// Declares an enumeration named FutureState. Enums are used to define a collection of named constants.
enum FutureState { none, loading, loaded, error }
